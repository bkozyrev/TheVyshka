<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dtd.thevyshka" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/ToolbarTheme" >
        <activity
            android:name=".Activities.MainActivity"
            android:label="@string/app_name">
            <!--android:theme="@style/Base.AppTheme" >-->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
            android:name=".Activities.SingleMeetingActivity"
            android:label="@string/meeting">

        </activity>
        <activity
            android:name=".Activities.CreateMeetingActivity"
            android:label="@string/create_meeting">

        </activity>
        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="5ae243373109f1ace7addbf59ea3f559d8ed6e69" />
    </application>

    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
